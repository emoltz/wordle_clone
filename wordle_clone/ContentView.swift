import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            WordleView()
        }
        .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
